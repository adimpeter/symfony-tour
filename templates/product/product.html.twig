{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
    <h1>{{ product.title }}</h1>
    <p>
        {{ product.description }}
    </p>

    <div>
        <a href="{{ path('product') }}">Back</a>
    </div>
{% endblock %}